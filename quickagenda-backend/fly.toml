app = "quickagenda-backend"
primary_region = "arn"  # Europe, close to your Supabase eu-north-1

[build]
  dockerfile = "Dockerfile"

[env]
  SPRING_DATASOURCE_URL = "jdbc:postgresql://db.YOUR_SUPABASE_PROJECT_ID.supabase.co:5432/postgres?sslmode=require"
  SPRING_DATASOURCE_USERNAME = "postgres"
  SPRING_DATASOURCE_PASSWORD = "YOUR_POSTGRE_PASSWORD"
  SERVER_ADDRESS = "0.0.0.0"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
